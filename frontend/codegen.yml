overwrite: true
schema: http://localhost:1338/graphql
documents: './src/**/*.graphql'
generates:
  ./src/types.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
